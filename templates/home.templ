package templates

import "app/config"

templ Home(tr func(key string) string) {
	<section>
		<div class="container">
			<p>templates/home.templ:</p>
			<p>{ tr("hello_world") }</p>
			<p>{ tr("message") }</p>
		</div>
		<a href={ config.RootPrefix + "login" }>Click to login</a>
	</section>
}
